name: x86-64 Pull and Save Docker Image

on:
  workflow_dispatch:
    inputs:
      docker_images:
        description: '请输入 Docker 镜像名称（多个用英文逗号分隔）'
        required: true
        default: 'alpine:latest'

jobs:
  pull_and_package:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Clean up Docker to free space
      run: |
        set -x
        docker system prune -a -f
        docker volume prune -f

    - name: Login to Aliyun Registry (optional)
      env:
        DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
        DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      run: |
        set -x
        if [ -n "$DOCKER_USERNAME" ] && [ -n "$DOCKER_PASSWORD" ]; then
          echo "🔐 Logging in to Aliyun Registry..."
          echo "$DOCKER_PASSWORD" | docker login registry.cn-hangzhou.aliyuncs.com -u "$DOCKER_USERNAME" --password-stdin
        else
          echo "⚠️  Skipping login: secrets not configured."
        fi

    - name: Pull, Save and Package Docker Images (x86-64)
      id: build
      run: |
        set -x
        images="${{ github.event.inputs.docker_images }}"
        IFS=',' read -r -a image_array <<< "$images"

        mkdir release_files

        FILE_LIST=""
        MIRROR_LIST=""

        for image in "${image_array[@]}"; do
          echo "🚀 Pulling ${image} for linux/amd64 (x86-64) ..."
          docker pull --platform linux/amd64 "${image}"

          SAFE_NAME="${image//\//_}"
          SAFE_NAME="${SAFE_NAME//:/_}"

          TAR_NAME="${SAFE_NAME}-x86-64.tar"
          TAR_GZ_NAME="${SAFE_NAME}-x86-64.tar.gz"

          docker save "${image}" -o "${TAR_NAME}"
          echo "📦 Saved as ${TAR_NAME}"

          tar -czf "${TAR_GZ_NAME}" "${TAR_NAME}"
          mv "${TAR_GZ_NAME}" release_files/

          FILE_LIST="${FILE_LIST} release_files/${TAR_GZ_NAME}"
          MIRROR_LIST="${MIRROR_LIST}\n- ${image}"

          rm -f "${TAR_NAME}"
        done

        echo "file_list=${FILE_LIST}" >> $GITHUB_OUTPUT
        echo "mirror_list=${MIRROR_LIST}" >> $GITHUB_OUTPUT

    - name: Upload to GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: x86-${{ github.run_number }}
        name: "x86-64 Docker Images"
        body: |
          自动构建的 x86-64 镜像包  
          **本次构建镜像：**
          ${{ steps.build.outputs.mirror_list }}
        files: ${{ steps.build.outputs.file_list }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

